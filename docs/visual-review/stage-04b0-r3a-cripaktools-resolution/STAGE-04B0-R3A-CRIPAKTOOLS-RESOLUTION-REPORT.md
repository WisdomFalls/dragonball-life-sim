# Stage 04B.0-R3A — CriPakTools verification and CPK listing`n`nThe official `Build/CriPakTools.exe` was acquired from `esperknight/CriPakTools` at commit `0dbd2229f7c3b519c67d65e89055fd4e610d6dca`. SHA-256: `11C979A4AC574E8BFC9E12294972CA19436DDAACBBDF8DBF2E8A0E16F7693773`; size 34,304 bytes. README documents listing with a single input CPK argument and separates extraction/replacement modes.`n`nRead-only listing succeeded with exit code 0 for `data0.cpk`, `data1.cpk`, `data2.cpk`, and `data2_develop.cpk`. `data1.cpk` contained `data/ui/texture/SYM.emb`, but none of these targeted listings exposed `data/chara/HUM/` or `data/chara/SYM/` facial resources. The `data.cpk` run emitted the header and initial entries but did not produce a complete usable listing, so it was not treated as resolved. No extraction was attempted. Archive size and SHA-256 were unchanged for the actively tested archive.`n`nVerdicts: `CRIPAKTOOLS_PREBUILT_FOUND=YES`; `CPK_TOOL_PROVENANCE=GREEN`; `CPK_TOOL_ACQUISITION=GREEN`; `CPK_LISTING=YELLOW`; `TARGET_CPK_IDENTIFIED=NO`; `VANILLA_CPK_EXTRACTION=NOT_RUN`; `HUM_RESOURCE_INVENTORY=NOT_RUN`; `HUM_SYM_RESOURCE_RELATIONSHIP=NOT_RUN`; `HUM_BCS_RESOLUTION=NOT_RUN`; `HUM_FACE_COMPONENT_SET=NOT_RUN`; `BLENDER_HEAD_IMPORT=NOT_RUN`; `NEUTRAL_HEAD_ASSEMBLY=NOT_RUN`; `HEAD_LANDMARK_MEASUREMENT=NOT_RUN`; `VANILLA_VARIATION_ENVELOPE=NOT_RUN`; `LUCEUS_COEXISTENCE_MEASUREMENT=NOT_RUN`; `HAIR_HEAD_REGISTRATION_CONTRACT=YELLOW`; `READY_FOR_CANONICAL_U7_HEAD_PROXY=NO`; `GIT_METADATA_WRITE_FOR_CODEX=BLOCKED_EXPECTED`.`n`nSingle blocker: identify the archive containing the HUM facial resources; current safe listings did not locate it and `data.cpk` did not yield a complete listing.
