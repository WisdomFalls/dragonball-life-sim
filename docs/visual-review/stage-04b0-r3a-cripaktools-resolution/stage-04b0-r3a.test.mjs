import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs"; test("CriPakTools provenance and listing evidence",()=>{const p=JSON.parse(fs.readFileSync("docs/visual-review/stage-04b0-r3a-cripaktools-resolution/cripaktools-provenance.json")); const r=JSON.parse(fs.readFileSync("docs/visual-review/stage-04b0-r3a-cripaktools-resolution/cpk-listing-results.json")); assert.equal(p.provenance,"official_github"); assert.equal(p.read_only_listing_verified,true); assert.equal(r.source_unchanged,true); assert.equal(r.extraction_performed,false);});
