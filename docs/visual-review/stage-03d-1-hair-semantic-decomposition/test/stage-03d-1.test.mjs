import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const m=JSON.parse(fs.readFileSync("docs/visual-review/stage-03d-1-hair-semantic-decomposition/compact-offset-front-decomposition-manifest.json"));test("decomposition contract",()=>{assert.equal(m.hair_identity_id,"mc-hair-01-compact-offset");assert.equal(m.direction,"front");assert.equal(m.components.length,3);assert.ok(m.components.every(c=>c.source_derived));});
