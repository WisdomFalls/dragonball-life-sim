import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs"; test("XV2INS inventory records no approved extractor",()=>{const x=JSON.parse(fs.readFileSync("docs/visual-review/stage-04b0-r2a-existing-tooling-resolution/tooling-inventory.json")); assert.equal(x.exists,true); assert.equal(x.yace_present,false); assert.equal(x.cpk_extractor_present,false);});
