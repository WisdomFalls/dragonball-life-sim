import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs"; test("controlled extraction inventory is local-only and complete",()=>{const i=JSON.parse(fs.readFileSync("docs/visual-review/stage-04b0-r3b-r1-controlled-extraction/hum-face-resource-inventory.json")); const e=JSON.parse(fs.readFileSync("docs/visual-review/stage-04b0-r3b-r1-controlled-extraction/extraction-integrity.json")); assert.equal(e.authorized,true); assert.equal(e.source_size_unchanged,true); assert.equal(i.component_counts.Face_base,131); assert.equal(i.provenance,"official_extracted");});
