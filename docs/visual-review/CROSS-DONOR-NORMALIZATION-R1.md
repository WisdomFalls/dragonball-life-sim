# Stage 03D.2E-R1 canonical head-space normalization

Hybrid 01 is preserved as failed diagnostic evidence: it combined two complete meshes whose source Z ranges were `0.605361..1.077079` (HUM_015) and `0.294700..0.598746` (Black Sister), demonstrating coexistence rather than head-space compatibility.

This correction defines one donor-independent authoring head space with `+Z` up, `-Y` front, `+Y` back, `-X` anatomical LEFT, and `+X` anatomical RIGHT. Root-anchor translations are derived independently: HUM_015 translates Z by `-0.105361`; Black Sister translates Z by `+0.205300`. No non-uniform scaling or morphology distortion is applied.

Black Sister remains a single `HAIR_Top` mesh (1,747 vertices / 2,477 polygons). It has not yet been split below object/submesh level, so the complete mesh is not classified as `front_fringe`. Single-mesh semantic extraction and Hybrid 02 are therefore intentionally pending topology inspection. Current verdicts: `CANONICAL_HEAD_SPACE=YELLOW`, `BLACK_SISTER_NORMALIZATION=YELLOW`, `SINGLE_MESH_SEMANTIC_EXTRACTION=UNPROVEN`, `BLACK_SISTER_RECONSTRUCTION=UNPROVEN`, `COMMON_SEMANTIC_GRAMMAR=UNPROVEN`, `CROSS_DONOR_GEOMETRIC_COMPATIBILITY=UNTESTED`, `CROSS_DONOR_ARTISTIC_COMPATIBILITY=UNTESTED`, `MODULAR_3D_HAIR_PIPELINE=YELLOW`.
