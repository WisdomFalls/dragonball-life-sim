import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs"; test("R3B localizes HUM in data.cpk without extraction",()=>{const x=JSON.parse(fs.readFileSync("docs/visual-review/stage-04b0-r3b-hum-archive-localization/cpk-search-results.json")); const d=JSON.parse(fs.readFileSync("docs/visual-review/stage-04b0-r3b-hum-archive-localization/data-cpk-diagnostic.json")); assert.equal(x.data_cpk.hum_hits>0,true); assert.equal(d.complete_listing,true);});
