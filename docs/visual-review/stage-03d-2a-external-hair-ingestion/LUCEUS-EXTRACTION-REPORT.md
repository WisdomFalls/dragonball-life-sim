# Stage 03D.2G — Luceus extraction review

Donor C is locked to `tryzick-hum015-luceus` (Tryzick HUM_015 — Luceus). Hair A remains `hair-a-resource-hum015` (Resource-Hair HUM_015).

The existing R1 Luceus front and 3/4 renders are preserved. A derived-copy extraction attempt was prepared, but the portable Blender headless session could not expose the installed ProjectXIR operator after reopening the Hair A scene (`bpy.ops.xenoverse_ir.emd` unavailable; addon activation from the portable session returned `ModuleNotFoundError: Project XIR`). No source geometry was modified and no unsupported raster masking or fabricated semantic boundary was introduced.

Therefore the semantic boundary is **UNSUPPORTED** in this pass. `LUCEUS_SINGLE_MESH_SEMANTIC_EXTRACTION=RED` and the required Hybrid Hair Test 02 was not created. This is an explicit stop under the stage stop condition; compatibility and artistic verdicts remain `UNRESOLVED` pending a working derived-copy import session. Existing Black Sister, Hybrid 01, B-Sha, and Tryzick reconnaissance evidence remains intact.
