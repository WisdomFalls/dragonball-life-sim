import test from "node:test";import assert from "node:assert/strict";import {readFileSync} from "node:fs";const d=JSON.parse(readFileSync(new URL("../docs/visual-review/stage-03c-natural-hair-catalogue/hair-catalogue.json",import.meta.url)));test("hair scaffold",()=>{assert.equal(d.owner,"BODY");assert.equal(d.identities.length,0);assert.equal(d.natural_palettes.length,4);assert.ok(d.approval_states.includes("review"));});
